// Deploy script will automatically replace the '<stampName>' with the actual stamp name
// namePrefix will be prepended to all your resource names

using '../main.bicep'

param namePrefix = '<stampName>' // E.g. mnilsen
