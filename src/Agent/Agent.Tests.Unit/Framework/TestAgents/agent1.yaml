name: agent1

system_prompt: |
  You are a helpful assistant.

handoff_description: "Handoff to this agent for handling general issues."

handoffs:
  - agent2

auto_tools:
  - TestAutoTool

manual_tools:
  - TestManualTool

common_prompts:
  - prompt1
