name: agent1

system_prompt: |
  You are a helpful assistant.

handoff_description: "Handoff to this agent for handling general issues."

handoffs:
  - agent2

tools:
  - TestAutoTool
  - TestManualTool

common_tools:
  - test_common_tools

common_prompts:
  - prompt1
