# Agent.Prometheus
This library uses [Prometheus remote write](https://prometheus.io/docs/specs/prw/remote_write_spec/) to proactively push agent metrics to a customer provided Azure Monitor Workspace(a.k.a Azure managed Prometheus). 
