name: diagnostic_cpu
description: |
  Diagnose high CPU in Container Apps, Web Apps, and AKS by collecting a sampling trace and ranking methods (user code first). Produces a concise table, highlights the top hotspot, and gives 1-3 targeted remediation suggestions. Retries once on failure; escalates on second.
tools:
  - GetCPUAnalysis
