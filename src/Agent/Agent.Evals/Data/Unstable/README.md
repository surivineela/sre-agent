This folder is for **unstable** agent test traces — runs that may be useful for evaluation and debugging, but are not expected to pass deterministically every time.

If you have a trace that reproduces an issue or captures an important behavior but is flaky, put it here rather than in stable/CI-gated datasets.
